<%= form_for(@plantclub) do |f| %>
  <% if @plantclub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plantclub.errors.count, "error") %> prohibited this plantclub from being saved:</h2>

      <ul>
      <% @plantclub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
